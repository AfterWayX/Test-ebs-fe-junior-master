import React from 'react';
import { render } from 'react-dom';
import App from './app'
import './index.css'

render(
    <section>
        <App />
    </section>
    , document.getElementById('root')
    );
